p#notice.flash = notice
h5
  = @author.name
- if @author.birth
  p
    strong Birth:
    = @author.birth
- if @author.death
  p
    strong Death:
    = @author.death
- if @author.about
  p
    strong About:
    = @author.about
p
  - @author.quotes.each do |quote|
    div = link_to quote.text, quote, class: 'quote-text'
    - if quote.source&.title.present?
      div.pull-right
        | #{quote.source&.title}

- @author.sources.each do |source|
  p = source.title

- if current_user&.admin?
  = link_to 'Edit', edit_author_path(@author), class: 'basic-link'
= link_to 'Authors', authors_path, class: 'basic-link'
